//javascript program to convert the first letter of a string to uppercase
let string = "peter parker here to pick up a passport please.";
console.log(string.replace(string[0], string[0].toUpperCase()));