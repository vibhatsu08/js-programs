//ES6 program to define a function in a program.
//older Javascript syntax to define a function.
let x = function addNumbers(x, y){
    return x + y;
}

//newer ES6 syntax to define functions using arrow functions.
let x = (x, y) => x + y;